services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./config:/app/config:ro
      - ./var/audit:/app/var/audit
    environment:
      - APP_ENV=development
    env_file:
      - .env
    profiles:
      - dev

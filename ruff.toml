line-length = 100
target-version = "py311"
exclude = [
    "trading_ig",
    "freqtrade",
    "sample",
    ".venv",
    # Pre-existing trading_ig test files use legacy import style; exclude until
    # they are migrated or the senzey_bots test suite has its own tests/unit/ etc.
    "tests",
]

[lint]
select = ["E", "W", "F", "I", "N", "UP"]

[lint.per-file-ignores]
# Streamlit page files use numeric prefixes (NN_name.py) by convention — N999 is expected
"src/senzey_bots/ui/pages/10_generate.py" = ["N999"]

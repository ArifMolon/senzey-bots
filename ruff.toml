line-length = 100
target-version = "py311"
exclude = [
    "trading_ig",
    "freqtrade",
    "sample",
    ".venv",
    # Pre-existing trading_ig test files use legacy import style; exclude until
    # they are migrated or the senzey_bots test suite has its own tests/unit/ etc.
    "tests",
]

[lint]
select = ["E", "W", "F", "I", "N", "UP"]
